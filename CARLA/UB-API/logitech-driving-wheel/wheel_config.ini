[G29 Racing Wheel]
# Axis indices (confirmed for Logitech MOMO Racing):
steering_wheel = 0
throttle = 1
brake = 2

# Button indices — confirmed live from this wheel:
#   face buttons = 0 and 1;  paddle shifters = 8 (left) and 9 (right).
# User chose the face buttons for reverse/handbrake.
# Run  python3 wheel_probe.py  to see the index printed for any button.
reverse = 1
handbrake = 0

# Camera / weather on wheel buttons are disabled (-1) for now; use the
# keyboard (Tab = camera view, C = weather) instead. Press a face button
# during wheel_probe.py and set its index here if you want them on the wheel.
camera = -1
weather = -1
restart = -1
